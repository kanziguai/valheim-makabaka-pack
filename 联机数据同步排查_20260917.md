# 联机（P2P，房主即服务端）数据同步排查 —— 2026-09-17

============================================================
一句话结论
============================================================
真正可能“丢东西”的不是网络代码，而是这三点：
  1) **各人机器上的 mod / 版本 / 实际 DLL 不一致**（版本号一样但文件不同，最难发现）
  2) **本机局部存档文件不在世界存档里**（SafeBox 保险柜、AzuEPI、Recycle 的 *_player_*.dat）
  3) **ChestFlow 自动喂食开着，同时“锁定/补货”功能被关掉并清理了旧标记** → 会被自动搬走的范围变大了

我们自研插件本身**没有**跨玩家直写容器/物品的代码（没有 ZNetView/RPC 直写，动物品都走游戏自己的
`Inventory.MoveItemToThis` / 原版 InventoryGui 流程），这部分是安全的。


============================================================
一、已核对、确认没问题（可以放心）
============================================================
1. **AzuEPI 装的是上游原版 2.4.14**：md5 `8d9f4ac43884e64fb0fafa08b04f609c`，与 Thunderstore
   下载的 2.4.14 包里的 DLL 逐字节一致 —— 没有把当年那版自编译（2.4.13 定制版）混进档里。
2. **堆叠类 mod 只留一个**：`shudnal-ItemStacksItemWeights` 启用、`hyr-ItemStacks` 禁用（两个同时开会打架，
   而且堆叠上限两边不一致时，超出的东西会被丢到地上甚至吞掉）。
3. **大量 mod 自带服务端同步**（配置以房主为准）：
   AzuEPI 62 项、EpicLoot 90 项、Hooked 93 项、Recycle_N_Reclaim 33 项、PetPantry 5 项、HoneyPlease 6 项
   标了 `[Synced with Server]`；另外 `shudnal-ConditionalConfigSync` 也在，能补上那些没自带同步的 mod。
4. **自研 InventorySortOnly**：只重排**自己背包**（收藏/收藏格受保护、有统计日志），不影响箱子与他人。
5. **自研 SafeBox / ChestFlowTweaks**：物品操作走游戏原生路径，没有直接写容器 ZDO；
   ChestFlowTweaks 还专门**干掉了“丢弃/垃圾桶”按钮**（少一个销毁入口）。
6. **外观与工具类不影响物品数据**：VRM 模型、VRMGhostFix、PortalBroadcastFix、MinimalUI、BetterUI 等。


============================================================
二、隐患清单（按风险从高到低）
============================================================

【高①】玩家之间的 mod 版本 / 实际 DLL 不一致 —— “东西不一样、箱子回滚”的头号原因
  · 为什么危险：包里 `mods.yml` 只是 r2modman 的元数据；你们**手动替换过 mod**（本机就有手动更新脚本），
    所以“版本号一样但 DLL 不同”完全可能发生；ChestFlow 官方明确要求**所有人同一版本**才能多人同时用一个箱子
    （它靠“把箱子的权威控制权在玩家间移交”来串行化修改）。
  · 典型表现：同一箱子两人看到的内容不同、刚放的物品回滚、EpicLoot 附魔/属性在不同人眼里不一样。
  · 对策：进服前所有人跑《联机一致性自检》，房主用 `-Compare` 比对，**重点看 DLL 段**。

【高②】本机局部存档文件不在世界存档里（房主备份也救不了）
  · `BepInEx\config\SafeBox\<玩家ID>_<角色名>.box`（+ `.bak`）：保险柜内容**只存在这台机器上**；
    而且配置里 `AutoStackPickups = true` —— 符合柜内物品的拾取物会**自动被吸进柜子**，
    等于一部分家当只活在这个文件里。
  · `AzuExtendedPlayerInventory_player_<id>.dat`：额外行/收藏/格子布局（本机文件）。
  · `Recycle_N_Reclaim_player_<id>.dat`：回收相关记录（本机文件）。
  · 典型表现：重装系统/换机/误删 config → 这些格子里的东西直接没了；
    SafeBox 读到损坏文件时会把文件改名成 `.bad` 并**从空柜开始**（需要手工把 `.bad` 改回 `.box`）。
  · 对策：每人定期把这些文件复制到别处（见第四节的一键备份命令）；**值钱的东西别只放保险柜**。

【高③】ChestFlow 自动喂食开着 + “锁定/补货”被关掉并清理旧标记
  · 已处置（2026-09-17）：**「多人同时开同一个箱子」已关闭** —— ChestFlow 自己这一项的默认值是 `true`，
    本包显式设为 `false`（`jg224.chestflow.cfg` → `[Multiplayer] AllowConcurrentChestUse = false`）。
    因为这一项**不带 `[Synced with Server]`**，不看房主配置，所以：
      1) 安装脚本新增 **5b) 强制校正必须一致的配置项**：每次安装/升级后都会把这一项校正为 false
         （升级模式会保留你自己的 config，所以必须在这里兜住），日志里会打印“已校正必须一致的配置项 N 处”；
      2) 《联机一致性自检》把 `jg224.chestflow.cfg` 列为 ★ 关键配置 —— 谁手动改回 true，一比对就能看出来。
  · 现状：`jg224.chestflow.cfg` → `[Auto Feed] Enabled = true / Range = 25 / ScanInterval = 5`
    （每 5 秒扫 25 米内的箱子并搬运物品）。
  · 现状：自研 `ChestFlowTweaks` 关闭了 Alt+左键的“锁定/补货”模式，并在启动时**清除所有旧的
    `ChestFlow.StackLocked` / `RestockId` / `RestockTarget` 标记**（连 `player.m_customData` 里的补货档案一起清）。
  · 风险：以前“锁住不让动”的物品，现在会被自动喂食带走；多人同场时自动喂食是并发的
    （ChestFlow 有串行化机制，但前提仍是“全员同版本”）。
  · **已决定（2026-09-17）：ChestFlow 的“锁定/补货”保持关闭，不用了。**
    保护物品统一走 **AzuEPI 收藏**：ChestFlowTweaks 已把 ChestFlow 的 `ItemLockService.IsLocked()` 挂到收藏上，
    所以**收藏＝锁定**（收藏过的物品 ChestFlow 一律不搬）。日志佐证：
      “已挂载 ItemLockService.IsLocked() Postfix，ChestFlow 现在认 AzuEPI 的收藏” / “未发现旧的 ChestFlow 标记，无需清理”。
    键位归属（避免再混淆）：**Alt+左键 = AzuEPI 收藏**（金色边框）；**Ctrl+左键 = SafeBox 的 MoveModifierKey**（背包↔保险柜）；
    **P = ChestFlow 快捷存取**；**Left Alt + S = ChestFlow 排序**；Alt+左键 的原版“锁定/补货”循环 = 已关闭。
  · 仍待定：**非房主的 Auto Feed**（Range 25 米 / 每 5 秒）要不要关掉或调小 —— 默认不动。

【中①】AzuEPI 额外 3 行 + 快速槽 3 + `Slots Use Armor = On`
  · 谁在中途把 AzuEPI 禁用/退回旧版本 → 额外格里的东西容易出问题（vanilla 不认识这些格子）。
  · 对策：写进规矩 —— **同一个世界进行中，不要禁用/升级 AzuEPI**；要换就全员一起换。

【中②】两个不同作者的 PlanBuild
  · 我们包用 `ShelledGhost-PlanBuild`；你另一个档里是 `MathiasDecrock-PlanBuild` —— **别混装**，
    蓝图数据与材料消耗的逻辑不同，会让“造完了东西却不对”。

【中③】键位冲突（只影响手感，不影响数据）
  · ChestFlow 的 `SortOpenChest = LeftAlt + S`；LeftAlt 同时是翻滚，Shift+Alt 还会触发输入法切换。
  · 对策：把 ChestFlow 的排序键改掉（我可以直接改配置）。

【低】已确认安全的
  · 自研插件无 RPC/ZDO 直写；InventorySortOnly 只影响自己背包；SafeBox 取出/存入走游戏 API。
  · ItemStacksItemWeights：配置锁定（房主权威）+ 堆叠表为空，与已禁用的 hyr-ItemStacks 不冲突。
  · 传送门、幽灵修复、外观类 mod 与物品数据无关。


============================================================
三、建议固定动作（写进群公告）
============================================================
1. **进服前**：所有人双击《联机一致性自检.bat》→ 把生成的 `一致性_xxx.txt` 和屏幕上的“指纹”发房主；
   房主跑一次 `-Compare` 比对（几秒钟），有差异的人用**完整安装包覆盖一次**再进。
2. **房主开世界前**：确认自己用的是最新包、当天没手动换过 mod。
3. **每周一次**：每人备份本机局部数据（见下）。
4. **铁规矩**：不在世界进行中启用/禁用/升级 mod；要更新就**全员同时**更新（用同一个包）。

    备份本机局部数据（在 PowerShell 里粘一行即可，会打到桌面）：
      $d="$env:USERPROFILE\Desktop\MAKABAKA本机数据_$(Get-Date -Format yyyyMMdd)"; `
      New-Item -ItemType Directory -Force $d | Out-Null; `
      Copy-Item "$env:APPDATA\r2modmanPlus-local\Valheim\profiles\MAKABAKA\BepInEx\config\SafeBox\*.box*" $d -ErrorAction SilentlyContinue; `
      Copy-Item "$env:APPDATA\r2modmanPlus-local\Valheim\profiles\MAKABAKA\BepInEx\config\*_player_*.dat" $d -ErrorAction SilentlyContinue; `
      Get-ChildItem $d | Measure-Object | Select-Object -Expand Count


============================================================
四、本次新增的工具：联机一致性自检
============================================================
  · `联机一致性自检.bat`（双击运行，非技术人员也能用）
  · `联机一致性自检.ps1`（同一件事，命令行用）

  它检查什么：
      1) mods.yml：名称 / 版本 / 启用状态（42 个包）
      2) 插件 DLL：**逐个算 MD5**（这条能抓出“版本号一样但文件不同”）
      3) BepInEx core 的 DLL
      4) 配置文件 MD5（★ 标记的 12 个是“必须一致”的关键配置：AzuEPI / EpicLoot / ChestFlow /
         ItemStacks / Recycle / PetPantry / HoneyPlease / FermenterUtilities / PlantEasily /
         AutoRepairBuilding / Hooked）
  输出：
      · 屏幕上一行**指纹**（把 mods + DLL + core 全部算成一个 sha256）
      · 同目录 `一致性_<电脑名>_<档名>_<日期>.txt` 明细（发给房主）

  房主比对：
      powershell -NoProfile -ExecutionPolicy Bypass -File .\联机一致性自检.ps1 -Compare 甲.txt,乙.txt,丙.txt

  已验证（在本机实测）：
      · 自检：42 个包（37 启用 / 5 禁用）、53 个插件 DLL、134 个配置文件 → 指纹正常输出
      · 自比：MOD / CORE / DLL / 关键配置 全部报“一致 ✓”
      · 跨档比对（拿另一个旧档当“不同步的队友”）：正确报出 44 项 mod 差异、58 个 DLL 差异、关键配置差异


============================================================
五、本版之前已经修过的（记录）
============================================================
  · PortalBroadcastFix 1.0.0：新放的传送门，队友不重连也能立刻看到（就是一次联机同步修复）
  · ChestFlowTweaks：关掉“丢弃/垃圾桶”按钮（减少物品销毁入口）、取消 Alt+左键 的锁定/补货循环
