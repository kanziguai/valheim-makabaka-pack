# 验证与演练：install.ps1 的四个"只看不改"开关

> 用途：不装任何东西，就能验证"安装脚本的流程对不对、装完之后状态对不对"。
> 所有开关都不写档内文件、不写游戏目录、不写模型缓存，可反复跑。
> 一键安装.bat 会把参数原样透传，所以 `一键安装.bat -Verify` 这样写也行（双击不行，要命令行）。

## 1. 四个开关

| 开关 | 看什么 | 什么时候用 |
|---|---|---|
| `-DetectOnly` | 只探测 profiles 目录 + 游戏目录，然后退出 | 怀疑"脚本找不到档/找不到游戏"时 |
| `-DryRun` | **整条流程的演练**：档内哪些文件会被新增/覆盖（逐条列出，最多 12 条）、VRM 三处会写什么、模型会下什么，然后退出 | 改完脚本、或升级包之前，想先确认"这次会动什么" |
| `-Verify` | **装完自检**：加载器插件 / shader / 18 个程序集 / 游戏 Managed 是否逐字节一致 / F9 面板 / 旧 loader 是否已禁用 / 模型库 / settings / F9 是否被占 | 装完、或出问题时自查；也可用来验收队友的机器 |
| `-ListModels` | 只列模型清单（★已缓存 / ↓需下载 + 大小 + 缓存目录） | 想确认"哪几只模型本机有了、要下多大" |

常用组合：

```bat
:: 升级前演练（推荐养成习惯）
一键安装.bat -DryRun
:: 装完自检
一键安装.bat -Verify
:: 只装 mod 那一段，完全不碰游戏目录（最安全）
一键安装.bat -ModsOnly -SkipVRM
:: 去网络变量、可重复的演练
一键安装.bat -DryRun -Offline -PackFile "%USERPROFILE%\Downloads\Valheim_MAKABAKA_包.zip"
```

## 2. 退出码

| 码 | 含义 |
|---|---|
| 0 | 正常结束；`-Verify` 时表示没有 ❌ 项（⚠️ 按需处理） |
| 1 | `-Verify` 有未通过项，或安装过程 `Fail`（日志在桌面） |
| 2 | 参数用法错误（例如指定了不存在的路径） |

## 3. 装完自检里怎么看结论

- ❌ 必须处理（例如"旧 loader 仍启用"：两个 loader 同时 patch `Player.Awake`，模型会错乱）
- ⚠️ 提示性（例如"未发现新加载器插件目录"= 你还在用旧的 ValheimVRM 1.2.2 方案，属正常）
- 详细日志：桌面 `MAKABAKA安装日志.txt`

## 4. -DryRun 已知会报出来的"包源脏东西"（v1.13 待清理）

演练时如果看到下面几条，说明**包源目录本身**还留着旧方案的文件，跑升级会把它们装回去：

| 计划里出现 | 为什么是问题 | 处理 |
|---|---|---|
| `新增 BepInEx\plugins\ValheimVRM_1.2.2\ValheimVRM.dll` | 旧 loader，会和新的 EnhancedValheimVRM 打架（档里现在是 `.dll.old` 禁用态，会被重新启用） | v1.13 从包源删掉该目录 |
| `新增 BepInEx\plugins\VRMGhostFix\VRMGhostFix.dll` | VRMGhostFix 已废弃（新加载器自带孤儿清理） | v1.13 从包源删掉 |
| `覆盖 BepInEx\JG224ModCore\reports\compatibility-report.txt(.bak)` | 运行期生成的文件不该进包源，升级会用旧快照覆盖新的 | 从包源删掉 `BepInEx\JG224ModCore\reports\` |

> 2026-09-19：上面三条已经**做成脚本里的自动保护**（见第 6 节）——装了新版加载器的档，跑这个包只会被维护、
> 不会被退回旧方案。包源目录本身的清理仍要做（v1.13），但不再会伤到已切到新版方案的机器。

## 6. 装了新版加载器的档：脚本会自动"维护新版"，不会把方案退回去

`install.ps1` 现在会先检测本档有没有 `BepInEx\plugins\Rawrtastic-EnhancedValheimVRM\EnhancedValheimVRM.dll`：

| 检测结果 | 脚本行为 |
|---|---|
| **有**（新版方案） | ① 不安装旧的 ValheimVRM 1.2.2；档里残留的 `ValheimVRM.dll` / `VRMGhostFix.dll` 自动停用（拷成 `.dll.old` 再删原文件，先备份到 `BepInEx\_vrm_backup_<时间>\`）<br>② 游戏 `valheim_Data\Managed\` 改为同步**新版加载器**的 18 个 UniVRM 程序集（替换前备份到 `Managed\_evv_prev`），不写旧包那 15 个<br>③ 模型与设置落 `<游戏>\EnhancedValheimVRM\<角色名>.vrm` + `settings_<角色名>.txt`<br>④ 复制档时用 robocopy `/XF` 排除 `ValheimVRM.dll` / `VRMGhostFix.dll` —— 连 `-ModsOnly` 也不会把它们拷回来；`-ModsOnly` 还会顺手停用残留 |
| 没有（旧方案，队友现状） | 行为与以前完全一样（装 1.2.2 + 15 个 dll + 模型进 `<游戏>\ValheimVRM\`） |

`-VRMOnly`（只换模型）承诺"不写档"，所以它只**报告**旧 loader 是否还启用，不动档里任何文件。

## 7. 为什么要这么改（2026-09-19 的真实事故）

旧包的 VRM 那一步会无条件把 `ValheimVRM 1.2.2` 的 15 个运行时 dll 写进游戏 `Managed`，
而新版加载器 EnhancedValheimVRM 1.3.1 要的是**更新版** UniVRM 程序集（同名不同版）。
实测后果：跑一次旧包，`Managed` 里 18 个有 14 个被换成旧版（一致只剩 4），两个 loader 同时留在档里，
下次启动游戏模型功能就可能坏掉。事后怎么一眼认出是谁改的：

```bash
# Managed 里的文件与"包内那 15 个"逐字节一致 ⇒ 就是旧包那一步写回去的
for n in VRM.dll UniGLTF.dll; do md5sum <游戏>/valheim_Data/Managed/$n; done
# mtime 反推是哪一次运行干的：包内文件会连自己的旧时间戳一起被拷进来（本次是 Sep 13 11:46）
ls -l <游戏>/valheim_Data/Managed/VRM.dll
# 每次运行留下的现场：Managed\_evv_prev（新版被覆盖前的备份）、<游戏>\_vrm_backup_<时间>（旧包替换前的备份）
```

修复（本次执行过的三步，全部可回退）：

```bash
bash "<备份目录>/同步EV程序集.sh" --apply     # 新版程序集：插件目录 → Managed（替换前备份到 _evv_prev）
# 档里 ValheimVRM_1.2.2\ValheimVRM.dll 与 VRMGhostFix\VRMGhostFix.dll 移出（.dll.old 保留在原位）
一键安装.bat -Verify                        # 应回到 10 ✅ / 0 ❌
```

## 8. 多模型入库：一次把模型都放本机，游戏里按 F9 随便切

因为游戏内已经能换模型（F9 面板），所以"装模型"不再等于"装一只"：

```
一键安装.bat -VRMOnly -AllModels                 # 把清单里【全部】模型补齐到本机模型库（推荐）
一键安装.bat -VRMOnly -Models "金乌-毛绒派对,菲比-反和谐"   # 只补指定的几个（名字从 -ListModels 抄）
一键安装.bat -ListModels                          # 先看清单 + "本机模型库已覆盖几个"
```
- 选中的模型**每一个**都会以清单名放进 `<游戏>\EnhancedValheimVRM\<清单名>.vrm`（不是只放第一只）→ 进游戏 F9 里就能逐个切
- `-AllModels` / `-LibraryOnly`：**不动"当前正在用的那只"**（`<角色名>.vrm` 保持原样）；想连当前用的一起换，加 `-CharName <角色名>`
- 目录里会生成/更新 `_模型名.txt`：显示名按清单编号命名（`03 安卡希雅-时之重奏 缘音回响`），与安装菜单、`Models\预览\` 参考图编号一一对应；改完重开 F9 面板即可生效
- **自动去重**：库里已有"同大小、只是名字不同"的文件时不再重复下载/入库（本次实测省掉 5 只 ≈ 500 MB 的重复下载）
- 幂等：再跑一次是 `新入库 0 个 / 已一致 N 个`，不会长第二份


## 9. 三步验证法（从浅到深）

1. `-DetectOnly` + `-ListModels`：环境与清单（秒级）
2. `-DryRun`：这次会动哪些文件（十几秒，档内文件逐个比 md5）
3. 真装一次（可选 `-ModsOnly` 分段）→ `-Verify` 出 ✅/⚠️/❌ 清单
   最后进游戏按 F9 换一次模型、看日志确认 `Loading [EnhancedValheimVRM …]` 与 `Loading [VRM Model Switcher …]` 各一次、无 Error。
