安装包：MAKABAKA_profile_v1.3_20260916.zip（约 138.0 MB；精确大小与 MD5/SHA256 见 Release 页的 SHA256SUMS.txt）
脚本日期：2026-09-16
适用游戏版本：英灵神殿 Valheim 1.0（Steam 正版）

================================================================================
v1.3 更新内容
================================================================================

【本版只改文档，包内容与 v1.2 完全相同】

1. 修正"方法二（git 一段命令）"的写法
   · 老写法把 if / elseif / else 写成三行分开，PowerShell 控制台是**逐行执行**的，
     "if (...) { ... }" 那一行本身已是完整语句，下一行的 elseif 会被当成新命令，
     报错：无法将"elseif"项识别为 cmdlet
   · 新写法每行都是独立完整语句，逐行粘、整段粘都安全：
       $d = "$env:USERPROFILE\valheim-makabaka-pack"
       if (-not (Test-Path "$d\.git") -and (Test-Path $d)) { $d = "$d-git" }
       if (Test-Path "$d\.git") { git -C $d pull } else { git clone https://github.com/kanziguai/valheim-makabaka-pack.git $d }
       & "$d\一键安装.bat"
   · 另外补了一个"一行版"（用 ; 分隔），两种写法都实测通过

2. 安装步骤.txt 的排查清单新增一条：
   「报「无法将"elseif"项识别为 cmdlet」→ 你把 if / elseif / else 分成三行粘了」

3. 说明文档同步（README.md / 安装步骤.txt / 安装指南.txt）

--------------------------------------------------------------------------------
v1.2 更新内容（回顾）
--------------------------------------------------------------------------------
· 下载安装包时**实时显示 百分比 / 当前速度 / 剩余时间**，完成给一行汇总（总大小/用时/平均速度）
  （根因修复：原来用 WebClient.DownloadFile 同步下载，它**不触发进度事件**，那段进度代码等于没显示）
· 下载**完整性校验**：收到的字节数 ≠ 应有字节数（断流/代理截断）即判失败，自动换下一条线路重试
· 首次运行可**选择安装包下载/解压位置**（不想放 C 盘的可以换盘；选过记住；模型缓存跟着走）；
  也可用参数 -DownloadDir D:\MAKABAKA_install
· 机器上有**多个 r2modman 数据文件夹**时，列出来让你选（带"已有档/上次装在这"标注；
  输 9 再扫描所有硬盘、输 0 自己输路径；只有一个时不会问）
· git 装法整合成一段命令（clone 或 pull 后**直接开始安装**，不用 cd、不用双击）

--------------------------------------------------------------------------------
v1.1 更新内容（回顾）
--------------------------------------------------------------------------------
· ValheimVRM 安装流程完善：插件 → r2modman 档里；运行时 15 个 dll → 游戏本体 valheim_Data\Managed；
  模型与设置 → 游戏本体 ValheimVRM\；逐文件 MD5 比对修复，装完打印"三处自检"
· **角色模型可选**：1) 金乌（59.8 MB，默认）　2) 辰星（36.7 MB）
· **角色名在脚本里输入**，脚本自动按 <角色名>.vrm + settings_<角色名>.txt 命名（不用自己找文件改名）
· 新增"只换模型"入口：换模型.bat（不重装档）

--------------------------------------------------------------------------------
安装步骤
--------------------------------------------------------------------------------
· 最省事：把 安装步骤.txt 发给朋友，照做即可（内含三种装法 + 每一步会问什么 + 排查清单）
· 一行命令（不用 git）：
    $u = "https://ghfast.top/https://raw.githubusercontent.com/kanziguai/valheim-makabaka-pack/main/install.ps1"
    $s = "$env:TEMP\makabaka-install.ps1"
    $wc = New-Object Net.WebClient; $wc.Headers.Add("User-Agent","makabaka-install")
    try { $wc.DownloadFile($u,$s) } catch { $wc.Proxy = $null; $wc.DownloadFile("https://raw.githubusercontent.com/kanziguai/valheim-makabaka-pack/main/install.ps1",$s) }
    powershell -NoProfile -ExecutionPolicy Bypass -File $s
· 完整文档：README.md（仓库） / 安装指南.txt（包内） / 安装步骤.txt（包内，最简版）

================================================================================
注意：本包不含任何个人角色名/个人状态文件；模型默认是"金乌"，角色名由脚本里输入决定。
================================================================================
